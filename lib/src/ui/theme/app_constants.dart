import 'package:flutter/material.dart';

class AppConstants {
  // BorderRadius
  static const BorderRadius borderRadius10 = BorderRadius.all(Radius.circular(10));
  static const BorderRadius borderRadius20 = BorderRadius.all(Radius.circular(20));
  static const BorderRadius borderRadius24 = BorderRadius.all(Radius.circular(24));

  // Padding
  static const EdgeInsets paddingAll16 = EdgeInsets.all(16.0);
  static const EdgeInsets paddingSymmetricHorizontal16 = EdgeInsets.symmetric(horizontal: 16.0);
  static const EdgeInsets paddingSymmetricVertical16 = EdgeInsets.symmetric(vertical: 16.0);

  // Sizes
  static const double iconSizeSmall = 16.0;
  static const double iconSizeMedium = 24.0;
  static const double iconSizeLarge = 32.0;

  // Others
  static const double bannerHeight = 400.0;
  static const double bannerWidth = 400.0;
}
